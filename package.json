{
  "name": "url-shortner-app",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd url-shortner-frontend && npm install && npm run build && cd ..",
    "deploy": "cp -a url-shortner-frontend/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'url-shortner-frontend built!'"
  }
}